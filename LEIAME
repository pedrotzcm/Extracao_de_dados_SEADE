Rode em um ambiente Linux:

docker compose down --volumes
docker build . -t airflow_teste
docker compose up -d
docker compose run airflow-webserver airflow db init
(Caso seja necessario acabar com outro processo que esta rodando na port, rode:
sudo lsof -i :5432 e sudo kill <PID>)
docker compose run airflow-webserver airflow users create \
    --username <username>\
    --password <senha> \
    --firstname <primeiro nome> \
    --lastname <sobrenome>\
    --role Admin \
    --email <email>
docker compose run airflow-webserver airflow db init


Com esses comandos rodados, acesse http://localhost:8080
